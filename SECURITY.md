# Security

Please report security issues privately to the project owner rather than opening a public issue.

The application processes local spreadsheets. Source files are read-only inputs by default, output
overwrite requires an explicit selection, and rule templates omit source/output paths. Review files
from untrusted senders in a protected environment because spreadsheet formats can contain external
links, macros, and embedded objects.
